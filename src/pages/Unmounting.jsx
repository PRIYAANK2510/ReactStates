const Unmounting = () => {
	return (
		<>
			<h1>Unmounting</h1>
			<p>
				This is the last phase in the component's lifecycle. As the name clearly suggests,
				the component gets unmounted from the DOM in this phase.{' '}
			</p>
		</>
	);
};
export default Unmounting;
